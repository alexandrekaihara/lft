# Docker commands 
DOCKER_COMMAND = "docker"
DOCKER_RUN = DOCKER_COMMAND + " run"

# Docker run options
NETWORK = "--network"
NAME = "--name"
PRIVILEGED = "--privileged"
DNS = "--dns"
MEMORY = "--memory"
CPUS = "--cpus"
